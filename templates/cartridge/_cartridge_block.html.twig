{% trans_default_domain 'cartridge' %}

{% if item.category.value == 'printer' %}
<div class="card border-0 shadow-sm mb-4">
    <div class="card-header bg-white py-3 d-flex justify-content-between align-items-center">
        <h5 class="card-title mb-0">
            <i class="bi bi-printer-fill me-2"></i>
            {{ 'title'|trans }}
        </h5>
        {% if not active_installation %}
            <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#installModalPrinter{{ item.id }}">
                <i class="bi bi-plus-lg me-1"></i>{{ 'actions.install'|trans }}
            </button>
        {% endif %}
    </div>

    <div class="card-body">
        {% if active_installation %}
            <div class="alert alert-{{ active_installation.cartridge.stockQuantity < 2 ? 'warning' : 'info' }} mb-4">
                <div class="d-flex justify-content-between align-items-start">
                    <div>
                        <h6 class="alert-heading mb-1">
                            <i class="bi bi-check2-circle me-1"></i>
                            {{ active_installation.cartridge.name }}
                            {% if active_installation.cartridge.color %}
                                <small class="text-muted">• {{ ('colors.' ~ active_installation.cartridge.color)|trans(domain: 'admin') }}</small>
                            {% endif %}
                        </h6>
                        <p class="mb-2 small">
                            {{ 'history.table.installed'|trans }}: {{ active_installation.installedAt|date('d.m.Y H:i') }}
                        </p>

                        {% if replacement_prediction and replacement_prediction.date %}
                            <div class="mt-2">
                                <span class="badge badge-{{ replacement_prediction.confidence == 'high' ? 'success' : (replacement_prediction.confidence == 'medium' ? 'warning' : 'secondary') }}">
                                    {% if replacement_prediction.method == 'pages_based' %}
                                        <i class="bi bi-bar-chart me-1"></i>{{ 'prediction.method_pages'|trans }}
                                    {% elseif replacement_prediction.method == 'time_based' %}
                                        <i class="bi bi-clock me-1"></i>{{ 'prediction.method_time'|trans }}
                                    {% else %}
                                        <i class="bi bi-info-circle me-1"></i>{{ 'prediction.method_estimate'|trans }}
                                    {% endif %}
                                </span>
                                <span class="ms-2 fw-semibold">
                                    {{ 'prediction.label'|trans }}: {{ replacement_prediction.date|date('d.m.Y') }}
                                </span>
                            </div>
                        {% endif %}
                    </div>

                    <form method="post"
                          action="{{ ea_url().setController('App\\Controller\\InventoryItemCrudController').setAction('removeCartridge') }}"
                          class="text-end"
                          onsubmit="return confirm('{{ 'confirm.remove_cartridge'|trans({}, 'messages') }}')">
                        <input type="hidden" name="_token" value="{{ csrf_token('remove' ~ active_installation.id) }}">
                        <input type="hidden" name="installation_id" value="{{ active_installation.id }}">
                        <div class="input-group input-group-sm mb-2" style="min-width: 200px;">
                            <input type="number" name="printed_pages" class="form-control" placeholder="{{ 'printed.pages_placeholder'|trans }}" min="0" value="{{ active_installation.printedPages }}">
                            <button class="btn btn-outline-danger" type="submit" title="{{ 'common.actions.remove'|trans(domain: 'messages') }}">
                                <i class="bi bi-eject"></i>
                            </button>
                        </div>
                        <small class="text-muted d-block">{{ 'messages.enter_pages'|trans }}</small>
                    </form>
                </div>
            </div>
        {% else %}
            <p class="text-muted mb-3">
                <i class="bi bi-info-circle me-1"></i>{{ 'messages.install_prompt'|trans }}
            </p>
        {% endif %}

        <h6 class="small text-muted text-uppercase mb-3">{{ 'history.title'|trans }}</h6>
        <div class="table-responsive">
            <table class="table table-sm table-borderless mb-0">
                <thead>
                    <tr class="small text-muted">
                        <th>{{ 'history.table.model'|trans }}</th>
                        <th>{{ 'history.table.installed'|trans }}</th>
                        <th>{{ 'history.table.removed'|trans }}</th>
                        <th>{{ 'history.table.page'|trans }}</th>
                    </tr>
                </thead>
                <tbody>
                    {% for inst in recent_installations %}
                    <tr class="{{ inst.installed ? 'fw-semibold' : '' }}">
                        <td>
                            {{ inst.cartridge.name }}{% if inst.installed %}
                            <span class="badge badge-success badge-sm ms-1">{{ 'status.active'|trans }}</span>{% endif %}
                        </td>
                        <td><small>{{ inst.installedAt|date('d.m.Y') }}</small></td>
                        <td><small>{{ inst.removedAt ? inst.removedAt|date('d.m.Y') : '—' }}</small></td>
                        <td><small>{{ inst.printedPages ? inst.printedPages|number_format(0, ',', ' ') : '—' }}</small></td>
                    </tr>
                    {% else %}
                    <tr>
                        <td colspan="4" class="text-center text-muted py-3 small">{{ 'messages.no_history'|trans }}</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endif %}